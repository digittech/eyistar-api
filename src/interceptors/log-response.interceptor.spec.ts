import { LogResponseInterceptor } from './log-response.interceptor';

describe('LogResponseInterceptor', () => {
  it('should be defined', () => {
    // expect(new LogResponseInterceptor()).toBeDefined();
  });
});
